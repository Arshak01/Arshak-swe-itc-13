cmake_minimum_required(VERSION 3.10)
project(calc)
file(GLOB SOURCES *)
add_subdirectory(sum)
add_subdirectory(sub)
add_subdirectory(mult)
add_subdirectory(divv)
add_executable(calc ${SOURCES})
target_link_libraries(calc sum sub mult divv)
